// Concatenate Two Strings

#include <stdio.h>
#include <string.h>

int main() {
    char str1[10] = "ahmet";
    char str2[10] = "ayvalik";

    strcat(str1, str2);
    printf("%s\n", str1);

    return 0;
}
